<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>365 Templates Magnéticos - Pare de Perder Vendas!</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Sora:wght@400;500;600;700;800&display=swap">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --dark: #0A0014;
            --gray: #A8A8A8;
            --light: #FFFFFF;
            --accent: #10002B;
            --darker: #05000A;
            --primary: #C77DFF;
            --secondary: #E0AAFF;
            --danger: #FF4444;
            --warning: #FFA500;
        }

        body {
            color: var(--light);
            background: var(--dark);
            overflow-x: hidden;
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
        }

        h1, h2, h3 {
            font-family: 'Sora', sans-serif;
            font-weight: 800;
        }

        .hero {
            display: flex;
            padding: 80px 20px 40px;
            overflow: hidden;
            position: relative;
            background: radial-gradient(ellipse at top, rgba(199, 125, 255, 0.15), transparent 70%),
                radial-gradient(ellipse at bottom right, rgba(224, 170, 255, 0.1), transparent 60%),
                var(--darker);
            min-height: 100vh;
            align-items: center;
            flex-direction: column;
            justify-content: center;
        }

        .hero::before {
            top: -50%;
            right: -20%;
            width: 800px;
            filter: blur(60px);
            height: 800px;
            content: '';
            position: absolute;
            animation: float 8s ease-in-out infinite;
            background: radial-gradient(circle, rgba(199, 125, 255, 0.2), transparent 70%);
            border-radius: 50%;
        }

        .badge {
            gap: 8px;
            color: var(--secondary);
            border: 1px solid rgba(199, 125, 255, 0.3);
            display: inline-flex;
            padding: 12px 24px;
            z-index: 1;
            position: relative;
            animation: fadeInUp 0.8s ease, pulse 2s ease infinite;
            font-size: 14px;
            background: rgba(199, 125, 255, 0.1);
            align-items: center;
            font-weight: 600;
            border-radius: 50px;
            margin-bottom: 24px;
            backdrop-filter: blur(10px);
        }

        .hero h1 {
            z-index: 1;
            position: relative;
            animation: fadeInUp 0.8s ease 0.2s both;
            font-size: clamp(32px, 6vw, 64px);
            max-width: 1100px;
            background: linear-gradient(135deg, #FFFFFF 0%, #C77DFF 50%, #E0AAFF 100%);
            text-align: center;
            line-height: 1.1;
            margin-bottom: 24px;
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            color: var(--gray);
            z-index: 1;
            position: relative;
            animation: fadeInUp 0.8s ease 0.4s both;
            font-size: clamp(18px, 2.5vw, 26px);
            max-width: 900px;
            text-align: center;
            margin-bottom: 40px;
            font-weight: 600;
        }

        .video-wrapper {
            width: 100%;
            margin: 40px 0;
            z-index: 1;
            position: relative;
            animation: fadeInUp 0.8s ease 0.6s both;
            max-width: 900px;
        }

        .video-container {
            overflow: hidden;
            position: relative;
            background: rgba(199, 125, 255, 0.05);
            box-shadow: 0 30px 80px rgba(199, 125, 255, 0.3);
            border-radius: 24px;
            backdrop-filter: blur(20px);
        }

        .video-container iframe {
            width: 100%;
            border: none;
            display: block;
            aspect-ratio: 16/9;
        }

        .cta-button {
            gap: 12px;
            color: var(--light);
            border: none;
            cursor: pointer;
            display: inline-flex;
            padding: 22px 48px;
            overflow: hidden;
            position: relative;
            animation: fadeInUp 0.8s ease 0.8s both, pulse 2s ease infinite;
            font-size: 20px;
            background: linear-gradient(135deg, #C77DFF 0%, #9D4EDD 100%);
            box-shadow: 0 20px 40px rgba(199, 125, 255, 0.4);
            transition: all 0.3s ease;
            align-items: center;
            font-weight: 700;
            border-radius: 16px;
            justify-content: center;
            text-decoration: none;
        }

        .cta-button:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 25px 50px rgba(199, 125, 255, 0.5);
        }

        .countdown {
            gap: 16px;
            margin: 32px 0;
            display: flex;
            z-index: 1;
            position: relative;
            animation: fadeInUp 0.8s ease 1s both;
        }

        .countdown-item {
            border: 1px solid rgba(199, 125, 255, 0.3);
            display: flex;
            padding: 16px 24px;
            min-width: 80px;
            background: rgba(199, 125, 255, 0.1);
            align-items: center;
            border-radius: 16px;
            flex-direction: column;
            backdrop-filter: blur(10px);
        }

        .countdown-number {
            color: var(--secondary);
            font-size: 32px;
            font-family: 'Sora', sans-serif;
            font-weight: 800;
        }

        .countdown-label {
            color: var(--gray);
            font-size: 12px;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .scarcity-box {
            border: 2px solid var(--danger);
            margin: 32px 0;
            padding: 24px;
            z-index: 1;
            position: relative;
            background: rgba(255, 68, 68, 0.1);
            text-align: center;
            border-radius: 16px;
            backdrop-filter: blur(10px);
        }

        .scarcity-box .spots-remaining {
            color: var(--danger);
            display: block;
            font-size: 18px;
            margin-top: 16px;
            font-weight: 700;
        }

        .section {
            padding: 100px 20px;
            position: relative;
        }

        .section.dark {
            background: var(--darker);
        }

        .container {
            margin: 0 auto;
            max-width: 1200px;
        }

        .section-header {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-title {
            font-size: clamp(28px, 5vw, 48px);
            line-height: 1.2;
            margin-bottom: 16px;
        }

        .section-subtitle {
            color: var(--gray);
            margin: 0 auto;
            font-size: clamp(16px, 2vw, 20px);
            max-width: 700px;
        }

        .loss-calculator {
            border: 2px solid var(--danger);
            margin: 40px auto;
            padding: 40px;
            max-width: 800px;
            background: rgba(255, 68, 68, 0.05);
            font-size: 20px;
            text-align: left;
            border-radius: 20px;
        }

        .loss-calculator .big-loss {
            color: var(--danger);
            display: block;
            font-size: 28px;
            margin: 8px 0 24px;
            font-weight: 800;
        }

        .urgency {
            color: var(--warning);
            padding: 20px;
            font-size: 24px;
            background: rgba(255, 165, 0, 0.1);
            text-align: center;
            font-weight: 700;
            border-radius: 16px;
        }

        .social-proof-numbers {
            gap: 40px;
            display: flex;
            margin: 60px 0;
            flex-wrap: wrap;
            justify-content: center;
        }

        .stat {
            display: flex;
            text-align: center;
            flex-direction: column;
        }

        .stat .number {
            color: var(--secondary);
            font-size: 56px;
            font-family: 'Sora', sans-serif;
            font-weight: 900;
        }

        .stat .label {
            color: var(--gray);
            font-size: 16px;
        }

        .grid {
            gap: 32px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        }

        .card {
            border: 1px solid rgba(199, 125, 255, 0.2);
            padding: 40px;
            background: rgba(199, 125, 255, 0.05);
            transition: all 0.3s ease;
            border-radius: 20px;
            backdrop-filter: blur(10px);
        }

        .card:hover {
            transform: translateY(-8px);
            background: rgba(199, 125, 255, 0.1);
            box-shadow: 0 20px 40px rgba(199, 125, 255, 0.3);
        }

        .value-stack {
            border: 2px solid var(--primary);
            margin: 40px auto;
            padding: 40px;
            max-width: 700px;
            background: rgba(199, 125, 255, 0.05);
            font-size: 20px;
            border-radius: 20px;
        }

        .value-stack h3 {
            margin-bottom: 24px;
            text-align: center;
        }

        .total-value {
            color: var(--secondary);
            margin: 32px 0;
            padding: 20px;
            font-size: 36px;
            background: rgba(199, 125, 255, 0.1);
            text-align: center;
            font-weight: 900;
            border-radius: 12px;
        }

        .strike-through {
            color: var(--gray);
            font-size: 24px;
            text-align: center;
            text-decoration: line-through;
        }

        .pricing-card {
            border: 2px solid var(--primary);
            margin: 60px auto;
            padding: 60px 40px;
            overflow: hidden;
            position: relative;
            max-width: 600px;
            background: linear-gradient(135deg, rgba(199, 125, 255, 0.1), rgba(224, 170, 255, 0.05));
            box-shadow: 0 30px 60px rgba(199, 125, 255, 0.3);
            text-align: center;
            border-radius: 32px;
        }

        .pricing-badge {
            top: 24px;
            color: var(--accent);
            right: 24px;
            padding: 8px 20px;
            position: absolute;
            font-size: 14px;
            background: linear-gradient(135deg, #FFD700, #FFA500);
            box-shadow: 0 5px 15px rgba(255, 215, 0, 0.4);
            font-weight: 800;
            border-radius: 50px;
        }

        .price-new {
            font-size: 72px;
            background: linear-gradient(135deg, #FFFFFF, var(--secondary));
            font-family: 'Sora', sans-serif;
            font-weight: 900;
            margin: 32px 0;
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .guarantee {
            gap: 16px;
            color: var(--gray);
            display: flex;
            font-size: 14px;
            border-top: 1px solid rgba(199, 125, 255, 0.2);
            margin-top: 24px;
            align-items: center;
            padding-top: 24px;
            justify-content: center;
        }

        .after-purchase {
            border: 2px solid var(--primary);
            margin: 60px auto;
            padding: 60px 40px;
            max-width: 800px;
            background: rgba(199, 125, 255, 0.05);
            font-size: 20px;
            border-radius: 24px;
        }

        .after-purchase h2 {
            margin-bottom: 32px;
            text-align: center;
        }

        .highlight {
            color: var(--secondary);
            margin-top: 32px;
            padding: 20px;
            background: rgba(199, 125, 255, 0.1);
            text-align: center;
            font-size: 22px;
            font-weight: 700;
            border-radius: 12px;
        }

        .last-push {
            padding: 80px 20px;
            background: var(--darker);
            text-align: center;
        }

        .big-question {
            color: var(--secondary);
            margin: 32px 0;
            font-size: clamp(24px, 4vw, 36px);
            font-weight: 700;
        }

        .comparison {
            margin: 32px auto;
            font-size: 22px;
            max-width: 600px;
            line-height: 1.8;
        }

        .final-cta {
            gap: 12px;
            color: var(--light);
            border: none;
            cursor: pointer;
            display: inline-flex;
            padding: 24px 56px;
            margin-top: 32px;
            font-size: 22px;
            background: linear-gradient(135deg, #C77DFF 0%, #9D4EDD 100%);
            box-shadow: 0 20px 40px rgba(199, 125, 255, 0.4);
            transition: all 0.3s ease;
            align-items: center;
            font-weight: 700;
            border-radius: 16px;
            justify-content: center;
            text-decoration: none;
        }

        footer {
            padding: 40px 20px;
            background: var(--darker);
            border-top: 1px solid rgba(199, 125, 255, 0.1);
            text-align: center;
        }

        footer p {
            color: var(--gray);
            margin-bottom: 16px;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            50% { transform: translate(-30px, 30px) rotate(5deg); }
        }

        @keyframes pulse {
            0%, 100% { box-shadow: 0 20px 40px rgba(199, 125, 255, 0.4); }
            50% { box-shadow: 0 20px 60px rgba(199, 125, 255, 0.6); }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (max-width: 768px) {
            .grid { grid-template-columns: 1fr; }
            .countdown { flex-wrap: wrap; justify-content: center; }
            .section { padding: 60px 20px; }
            .social-proof-numbers { gap: 20px; }
            .stat .number { font-size: 42px; }
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="badge">⚡ OFERTA LIMITADA - 75% OFF</div>
        <h1>Pare de Perder Vendas Por Não Saber o Que Postar! 365 Templates Que Vendem Sozinhos</h1>
        <p class="hero-subtitle">Transforme Seguidoras em Clientes Pagantes em 7 Dias (Sem Gastar Horas Criando Conteúdo do Zero)</p>
        
        <div class="video-wrapper">
            <div class="video-container">
                <iframe src="https://player-vz-120649cf-96b.tv.pandavideo.com.br/embed/?v=13f798f1-e4fc-42f2-a0b7-f32c84048c63&saveProgress=false&controls=play-large" allowfullscreen></iframe>
            </div>
        </div>

        <a href="#pricing" class="cta-button">
            SIM! QUERO PARAR DE PERDER VENDAS AGORA →
        </a>

        <div class="countdown">
            <div class="countdown-item">
                <div id="hours" class="countdown-number">23</div>
                <div class="countdown-label">Horas</div>
            </div>
            <div class="countdown-item">
                <div id="minutes" class="countdown-number">59</div>
                <div class="countdown-label">Minutos</div>
            </div>
            <div class="countdown-item">
                <div id="seconds" class="countdown-number">59</div>
                <div class="countdown-label">Segundos</div>
            </div>
        </div>

        <div class="scarcity-box">
            ⚠️ ATENÇÃO: Apenas 47 vagas restantes neste preço
            <br><br>
            Após isso, o valor sobe para R$ 197
            <span class="spots-remaining">
                🔴🔴🔴🔴🔴🔴🔴⚪⚪⚪ 68% das vagas já preenchidas
            </span>
        </div>
    </section>

    <!-- Problem Section -->
    <section class="section">
        <div class="container">
            <div class="section-header">
                <div class="badge">💸 QUANTO ISSO ESTÁ CUSTANDO?</div>
                <h2 class="section-title">Você Sabe Quanto Dinheiro Está PERDENDO Agora?</h2>
            </div>

            <div class="loss-calculator">
                📉 Sem conteúdo estratégico por 30 dias = 
                <span class="big-loss">- R$ 8.400 em vendas perdidas</span>
                
                ⏰ 2 horas/dia tentando criar posts = 
                <span class="big-loss">60 horas/mês desperdiçadas</span>
                
                💸 Contratar social media = 
                <span class="big-loss">R$ 1.200 a R$ 3.000/mês</span>
            </div>

            <p class="urgency">
                ⚠️ Cada dia sem esses templates = R$ 280 deixados na mesa
            </p>

            <div style="text-align: center; margin-top: 60px;">
                <a href="#pricing" class="cta-button">
                    GARANTIR MINHA VAGA COM 75% OFF →
                </a>
            </div>
        </div>
    </section>

    <!-- Social Proof -->
    <section class="section dark">
        <div class="container">
            <div class="section-header">
                <div class="badge">📊 RESULTADOS REAIS</div>
                <h2 class="section-title">Mais de 2.800 Lojistas Já Transformaram Suas Vendas</h2>
            </div>

            <div class="social-proof-numbers">
                <div class="stat">
                    <span class="number">2.847+</span>
                    <span class="label">Lojistas Usando</span>
                </div>
                <div class="stat">
                    <span class="number">156%</span>
                    <span class="label">Aumento Médio em Vendas</span>
                </div>
                <div class="stat">
                    <span class="number">4,9★</span>
                    <span class="label">Avaliação Média</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Pricing -->
    <section id="pricing" class="section">
        <div class="container">
            <div class="section-header">
                <div class="badge">💎 OFERTA ESPECIAL</div>
                <h2 class="section-title">Veja o Valor Real do Que Você Está Levando</h2>
            </div>

            <div class="value-stack">
                <h3>Tudo Que Está Incluso:</h3>
                
                ✅ 365 Templates Profissionais → <strong>R$ 2.920</strong><br>
                ✅ Copywriting Testada e Aprovada → <strong>R$ 1.200</strong><br>
                ✅ Estratégia de Conteúdo Anual → <strong>R$ 3.600</strong><br>
                ✅ Sistema Reels Irresistíveis → <strong>R$ 497</strong><br>
                ✅ Guia Sua Marca Única → <strong>R$ 297</strong>
                
                <div class="total-value">
                    VALOR TOTAL: R$ 8.514
                </div>
                
                <div class="strike-through">
                    De R$ 388,00 por apenas...
                </div>
            </div>

            <div class="pricing-card">
                <div class="pricing-badge">75% OFF</div>
                
                <div class="price-new">R$ 97</div>

                <a href="https://payment.ticto.app/O511D0DFA" class="cta-button">
                    💳 SIM! QUERO GARANTIR AGORA
                </a>

                <div class="guarantee">
                    <span>🛡️ Pagamento 100% Seguro</span>
                    <span>|</span>
                    <span>✅ Garantia de 7 Dias</span>
                </div>
            </div>
        </div>
    </section>

    <!-- After Purchase -->
    <section class="section dark">
        <div class="container">
            <div class="after-purchase">
                <h2>O Que Acontece Nos Próximos 5 Minutos:</h2>
                
                ⚡ <strong>Minuto 1:</strong> Pagamento confirmado automaticamente<br><br>
                ⚡ <strong>Minuto 2:</strong> Email com acesso à área de membros<br><br>
                ⚡ <strong>Minuto 3:</strong> Download dos 365 templates organizados<br><br>
                ⚡ <strong>Minuto 4:</strong> Você edita seu primeiro template<br><br>
                ⚡ <strong>Minuto 5:</strong> Posta e começa a receber mensagens
                
                <p class="highlight">
                    🎯 Ainda hoje você já terá conteúdo profissional no ar gerando vendas!
                </p>
            </div>
        </div>
    </section>

    <!-- Last Push -->
    <section class="last-push">
        <div class="container">
            <h2 class="section-title">Última Pergunta Antes de Você Decidir:</h2>
            
            <p class="big-question">
                Quanto vale para você nunca mais ficar travada sem saber o que postar?
            </p>
            
            <p class="comparison">
                R$ 97 = Preço de 2 pizzas 🍕🍕
                <br><br>
                Mas os templates trabalham para você pelos próximos 12 meses gerando vendas todos os dias.
                <br><br>
                <strong>Você prefere gastar com pizza ou investir no crescimento da sua loja?</strong>
            </p>
            
            <a href="#pricing" class="final-cta">
                FAZER O MELHOR INVESTIMENTO DE R$ 97 DA MINHA VIDA →
            </a>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>© 2024 Templates Magnéticos - Todos os direitos reservados</p>
        <p>🛡️ Garantia de 7 dias | 💳 Pagamento 100% Seguro</p>
    </footer>

    <script>
        // Smooth Scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // Countdown Timer
        function updateCountdown() {
            const now = new Date();
            const tomorrow = new Date(now);
            tomorrow.setHours(24, 0, 0, 0);
            
            const diff = tomorrow - now;
            
            const hours = Math.floor(diff / (1000 * 60 * 60));
            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((diff % (1000 * 60)) / 1000);
            
            document.getElementById('hours').textContent = String(hours).padStart(2, '0');
            document.getElementById('minutes').textContent = String(minutes).padStart(2, '0');
            document.getElementById('seconds').textContent = String(seconds).padStart(2, '0');
        }

        updateCountdown();
        setInterval(updateCountdown, 1000);
    </script>
</body>
</html>